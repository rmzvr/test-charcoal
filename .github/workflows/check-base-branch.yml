name: Check base branch

on: [pull_request, workflow_dispatch]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Fail if base is not main
        if: github.event.pull_request.base.ref != 'main'
        run: |
          echo "PRs must target main, not a stacked branch."
          exit 1
